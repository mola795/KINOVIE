<h1 class="page-heading">Lists</h1>

<div class="row">
  <div class="col-md-12 text-center">
    <%= link_to 'New List', new_list_path, class: 'btn btn-primary mb-4' %>
  </div>
</div>

<% @lists.each do |list| %>
 <%= render 'shared/collection_card', list: list%>
<% end %>
